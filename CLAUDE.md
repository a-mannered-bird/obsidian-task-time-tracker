# Task Time Tracker plugin

Repository practices live in AGENTS.md so that every coding agent reads the
same file; it is imported here so Claude Code loads it automatically.

@AGENTS.md

Roadmap and recorded design decisions: PLAN.md (read it before starting a
phase; tick its checkboxes as work lands).
